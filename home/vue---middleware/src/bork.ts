import Store from '@/Store';
import { NavigationGuardNext, Route } from 'vue-router';

/**
 * Description of middleware
 *
 * @return void
 */
export function ${input.filename}(to: Route, from: Route, next: NavigationGuardNext): void {
	// Not implemented

	next();
}
